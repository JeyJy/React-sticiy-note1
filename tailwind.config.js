module.exports = {
    content: [
        'node_modules/flowbig-react/**/*.{js, jsx, ts, tsx}'
    ],
    theme: {
        extend: {},
    },
    plugins: [
        require('flowbite/plugin')
    ],
}